// 3. Partiendo del array que has construido en el ejercicio anterior,
// construye un algoritmo que filtre en un nuevo array
// las cartas rojas y pares. Luego imprime en la consola la
// última carta de ese nuevo array

// Tomamos código de ejer anterior
<script type="text/javascript">
var palos=['d','p','t','c'];
var cartasPorPalo=12;
var baraja= [];
for (var i = 0; i < palo.length; i++) {
  for (var j = 1; j < cartasPorPalo.length; j++) {
    var palo= palos[i];

    var carta= {
      valor:j,
      palo: palo[i]
    }
  ;
    listaCartas[cartas.length]=[carta];
  }
}
console.log(baraja);

var cartaNegra=[];
var cartaRoja=[];

for (var i = 0; i < baraja.length; i++) {
var carta=baraja[i];
if (carta.palo==="c"||carta.palo==="d") {
cartaNegra[cartaNegra.length]= carta;
}else {
cartaRoja[cartaRoja.length]=carta;
}
}
// Codigo específico de este ejercicio


var paloRojo =['c','d'];
var cartaParRoja = [];

for (var i = 0; i < deck.length; i++) {
  var carta = deck[i];
  car cardEsRoja = carta.palo === paloRojo[0]|| carta.palo === cartaRoja[1];
  var cartaEsPar = carta.Numero % 2 === 0;

  if (cartaEsRoja && cartaEsPar) {
    cartasRojasPares [cartasRojasPares.length]; // Incompletooooorr, miralo.
        
  }

}
